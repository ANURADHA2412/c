#include<stdio.h>
int findmax(int *);
void main()
{
  int a[5],i,max;
  for(i=0;i<=4;i++)
  {
    scanf("%d",&a[i]);
    }
    max = findmax(int *a)
    {
      int max = *a;
      for(i=1;i<=4;i++)
      {
        if(*(a+i)>max)
          max = *(a+i):
       }
        return max;
       }
      }
  
